language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-autobot' ]; then cd .. && eval "mv $currentfolder generator-autobot" && cd generator-autobot; fi

